<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nav_menu_bg_color"
    android:orientation="vertical"
    tools:context="sg.com.bumbox.bumbox.fragments.NavigationDrawerFragment">

    <ImageView
        android:id="@+id/imageView2"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingBottom="12dp"
        android:paddingTop="12dp"
        android:src="@drawable/ic_launcher" />

    <ListView
        android:id="@+id/drawer_list_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:choiceMode="singleChoice"
        android:divider="@color/app_grey_darker"
        android:dividerHeight="0.5dp"
        android:listSelector="@drawable/selector_nav_drawer_list"
        tools:context=".NavigationDrawerFragment" />
</LinearLayout>